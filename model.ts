interface BaseBlock {
    id?: number,
    name?: string,
    x?: number,
    y?: number,
    align?: string,
    width?: number,
    height?: number,
    imageUrl?: string,
    textContent?: string
}

